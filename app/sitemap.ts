import type { MetadataRoute } from 'next';
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://thestudyverse.co.uk';return ['/','/masterclasses','/masterclasses/gcse-chemistry','/community','/vip','/courses','/resources','/tutoring','/about','/contact','/privacy','/terms','/refunds','/cookies'].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:'monthly',priority:path==='/'?1:.7}));}
